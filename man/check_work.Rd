% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_work.R
\name{check_work}
\alias{check_work}
\title{Check a hand calculation against a derivation}
\usage{
check_work(x, value, step = NULL, tol = 0.005)
}
\arguments{
\item{x}{An \code{epibyhand_derivation}.}

\item{value}{Numeric. The value you computed by hand.}

\item{step}{Optional. Which step to compare against: a step number, or the
symbol of a step such as \code{"SE"} or \code{"R1"}. Defaults to the final estimate.}

\item{tol}{Relative tolerance for the comparison. The default is loose
enough to accept a value rounded to two decimal places.}
}
\value{
Invisibly, \code{TRUE} if the value matched the requested target.
}
\description{
Compare a value you computed by hand against the final estimate, or against
a named intermediate step. When the value does not match, \code{check_work()}
searches every step of the derivation for one that does -- so instead of
"wrong", the student is told \emph{where} they stopped or slipped.
}
\examples{
d <- odds_ratio(epi2x2(36, 14, 30, 25))
check_work(d, 2.14)
check_work(d, 2.571)
}
