% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/attributable.R
\name{attributable_fraction}
\alias{attributable_fraction}
\title{Attributable fraction, with worked derivation}
\usage{
attributable_fraction(
  x,
  ...,
  among = c("exposed", "population"),
  conf_level = 0.95
)
}
\arguments{
\item{x}{An \code{epi2x2}, or the first of four cell counts.}

\item{...}{Further cell counts if \code{x} is a bare count.}

\item{among}{\code{"exposed"} (the default) or \code{"population"}.}

\item{conf_level}{Confidence level for the interval.}
}
\value{
An \code{epibyhand_derivation}.
}
\description{
The proportion of disease that would be prevented by removing the exposure,
either among the exposed (\code{among = "exposed"}) or in the whole population
(\code{among = "population"}).
}
\details{
For the population fraction the derivation computes the same quantity three
ways -- directly from risks, by Levin's formula from exposure prevalence,
and by Miettinen's formula from the proportion of cases exposed -- and shows
that they agree. They are not competing estimators; they are one quantity
written three ways, which is easier to believe once seen numerically.
}
\examples{
attributable_fraction(epi2x2(36, 14, 30, 25))
attributable_fraction(epi2x2(36, 14, 30, 25), among = "population")
}
