% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mantel_haenszel.R
\name{homogeneity}
\alias{homogeneity}
\title{Breslow-Day test of homogeneity, with worked derivation}
\usage{
homogeneity(x, ..., tarone = TRUE)
}
\arguments{
\item{x}{An \code{epi_strata}, or arguments passed to \code{\link[=epi_strata]{epi_strata()}}.}

\item{...}{Further strata if \code{x} is not already an \code{epi_strata}.}

\item{tarone}{Apply Tarone's correction. Defaults to \code{TRUE}.}
}
\value{
An \code{epibyhand_derivation}.
}
\description{
Tests whether one common odds ratio can be assumed across strata -- the
assumption that makes \code{\link[=mh_odds_ratio]{mh_odds_ratio()}} meaningful. The statistic is a sum
of per-stratum contributions, and those contributions are shown, so a
single badly behaved stratum can be seen rather than inferred.
}
\details{
Tarone's correction is applied by default; without it the statistic is
slightly too large.
}
\examples{
homogeneity(epi_strata(c(10, 20, 5, 40), c(30, 15, 20, 25)))
}
